meta {
  name: Update
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/api/alerts/{{alertId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "channel": "slack"
  }
}
