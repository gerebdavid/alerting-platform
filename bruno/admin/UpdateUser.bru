meta {
  name: UpdateUser
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/api/admin/users/{{signupUserId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "isActive": false
  }
}
